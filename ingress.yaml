apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  namespace: ingress-tutorial
  name: sample-ingress
spec:
  ingressClassName: public
  defaultBackend:
    service:
      name: sample-service
      port:
        name: api